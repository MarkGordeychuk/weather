import * as React from "react";

import "/src/styles/Header.scss";


function Header() {
    return (
        <header>
            <div>Weather</div>
        </header>
    );
}

export default Header;